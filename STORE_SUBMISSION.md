# Pipeline Chrome 应用商店提交资料

## 商店文案

- 名称：`Pipeline`
- 摘要：`在 Claude、ChatGPT、Gemini、豆包和千问页面中开启并行对话工作区。`
- 类别建议：`生产力工具`
- 语言：`中文（简体）`

### 详细说明

Pipeline 为常用 AI 聊天平台提供页内并行对话工作区。

在受支持的网站中点击浏览器工具栏上的 Pipeline 图标，即可保留当前对话并打开新的空白对话窗格。你可以继续增加、聚焦、排序或关闭窗格，在同一页面中并排处理多个对话。

支持 Claude、ChatGPT、Gemini、豆包和千问。扩展不会收集、存储或向开发者传输聊天内容、浏览记录或使用数据。

## 隐私实践表单

- 单一用途：`在受支持的 AI 聊天网站中提供页内并行对话工作区。`
- 是否使用远程代码：`否`
- 是否收集用户数据：`否`
- `activeTab` 理由：`用户点击扩展图标时，扩展需要识别当前标签页并向该页面发送开启并行工作区的指令。`
- `declarativeNetRequest` 理由：`并行工作区以子框架显示受支持的 AI 聊天页面，需要仅对这些平台移除阻止子框架显示的响应头。`
- 主机权限理由：`内容脚本只在清单列出的五个 AI 聊天平台运行，用于创建和管理页内并行对话窗格。`
- 隐私政策：将 [PRIVACY.md](./PRIVACY.md) 发布到公开 HTTPS 页面后，把该页面地址填入开发者控制台。

## 上传清单

1. 在 `ai-chat-anchor` 目录运行 `npm run build`。
2. 上传生成的 `dist/pipeline-<版本号>.zip`。
3. 上传至少一张真实功能截图；建议使用 1280×800 或 640×400 PNG/JPEG。
4. 填写上方商店文案、权限理由与隐私实践。
5. 提供可公开访问的隐私政策 HTTPS 地址。
6. 选择发布范围并提交审核。

每次提交更新前，先递增 `manifest.json` 和 `package.json` 中的版本号，并保持两处一致。
