# Pipeline Chrome 扩展规范

## 1. 项目概述

- **项目名称**：Pipeline
- **类型**：Chrome 扩展程序
- **核心功能**：在支持的 AI 聊天页面中开启并行对话工作区
- **唯一入口**：点击浏览器工具栏中的 Pipeline 插件图标

## 2. 功能规范

### 2.1 支持平台

- Claude
- ChatGPT
- Gemini
- 豆包
- 千问

### 2.2 并行模式

- 点击插件图标后直接开启，不显示扩展弹窗或右侧控制面板
- 当前对话作为首个窗格保留
- 自动创建一个新的空白对话窗格，用户直接在窗格内输入
- 右上角提供加号按钮，用于继续增加空白窗格
- 右上角提供列表按钮，用于展开或折叠窗口列表
- 窗口列表只包含窗格导航、聚焦与删除操作，不包含输入区和模式开关
- 自动向新对话注入并发送问题
- 支持聚焦、排序和关闭窗格
- 支持关闭整个并行工作区并恢复原页面

### 2.3 明确移除的能力

- 不生成消息目录
- 不显示右侧问答刻度或目录入口
- 不提供消息搜索、消息跳转或滚动高亮
- 顶层页面不为目录功能扫描消息 DOM
- 不提供浏览器工具栏弹窗
- 不显示“新建对话”输入框或“并行模式”开关侧栏

## 3. 验收标准

- [ ] 插件成功安装并显示工具栏图标
- [ ] 在支持的平台点击图标后直接打开并行工作区
- [ ] 页面初始状态不显示消息目录或目录刻度
- [ ] 新问题可以创建并排对话并自动发送
- [ ] 并行窗格可以聚焦、排序和关闭
- [ ] 关闭并行工作区后不显示旧消息目录面板
- [ ] 在不支持的页面点击图标时显示错误徽标和提示标题
